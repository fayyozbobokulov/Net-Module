input: npm run bench path iteration runs
optput: TestName time(ms) ram (mb) cpu(%)


считать параметры
парсинг параметров
валидировать параметры
нормализовать параметры
    path
        ./
        ../
        ~/
        lopp.js
        loop
    iteration
        0
        -100
        +
открыть файл тестов
    проверить существование
    импортировать
    проверяешь соответствие структуры

каждую функцию прогнать runs раз по iteration раз
собрать результат для каждого прогона
вычислить средний результать
вывести результат для каждой функуции