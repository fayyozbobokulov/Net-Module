export async function valodatorPath(path: string, iteration: number, runs: number) {

  function validatePath(path: string) {
    
  }

  function validateIteration(iteration: number) {

  }

  function validateRuns(runs: number) {

  }

  
}